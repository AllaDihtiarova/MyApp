import styles from "../styles/screens/NotFound.module.css"

const NotFound = () => {
  return (
    <div className={styles.container}>
      <h2 className={styles.title}>Not found the page</h2>
      <div className={styles.image}/>
    </div>
  )
}

export default NotFound